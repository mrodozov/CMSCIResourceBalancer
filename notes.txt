1. Three types of objects

   - A singleton 'resource pool'. Checks CPU, RAM, disk, network availability
   on prompt. Returns current availability

   - A singleton ''

   - Class for prompting Elastic search API to ask for the stats history of each test of interest.
   For example relval number 1234.56 (unique id for it)

2. The


Note - add query for
        -- workflow. Say for a certain number workflow we need the last 5 records
        -- architecture. again, the last N records
        -- list of arguments ? each of which with list of params ? like this:
           -- {arg1:[par1,par2,par3],arg2:[par1,par2,par3]..argn[par1,par2..parN]}
